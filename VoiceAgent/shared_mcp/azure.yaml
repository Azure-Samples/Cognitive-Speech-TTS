# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: voice-agent-shared-mcp
metadata:
  template: voice-agent-shared-mcp
services:
  sharedmcp:
    project: .
    host: containerapp
    docker:
      path: Dockerfile
      context: .
      remoteBuild: true

